<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmdp.mapper.SeckillVoucherMapper">

    <update id="updateStock" parameterType="java.lang.Long">

                update tb_seckill_voucher
                set stock = stock - 1
               where voucher_id = #{voucher_id} and stock>0



    </update>
    <select id="selectVoucherForUpdate" parameterType="java.lang.Long" resultType="com.hmdp.entity.SeckillVoucher">
        SELECT * FROM tb_seckill_voucher
        WHERE voucher_id = #{voucher_id}
          AND stock > 0
            FOR UPDATE
    </select>

</mapper>
